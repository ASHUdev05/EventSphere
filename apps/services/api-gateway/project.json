{
  "name": "api-gateway",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "targets": {
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "./mvnw spring-boot:run",
        "cwd": "apps/services/api-gateway"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": ".\\mvnw.cmd clean package -DskipTests",
        "cwd": "apps/services/api-gateway"
      }
    }
  }
}
